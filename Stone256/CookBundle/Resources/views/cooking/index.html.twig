{% extends '::base.html.twig' %}

{% block body -%}
    <h1>cooking list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
		<!--
                <th>Fridge</th>
                <th>Recipes</th>
                -->
                <th>Food</th>
                <!--<th>Actions</th>-->
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cooking_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
<!--                
		<td>{{ entity.fridge }}</td>
                <td>{{ entity.recipes }}</td>
-->
                <td>{{ entity.food }}</td>
                <td>
                <ul>
                    
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cooking_new') }}">
                Recipes Finder 
            </a>
        </li>
    </ul>
    {% endblock %}
